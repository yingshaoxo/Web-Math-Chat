{
    "name": "WebMathChat",
    "version": "1.0.0",
    "main": "./src/index.js",
    "author": "yingshaoxo",
    "license": "MIT",
  
    "scripts": {
        "start": "cross-env NODE_ENV=development parcel serve ./src/index.html --no-hmr --open --out-dir build",
        "build": "cross-env NODE_ENV=production parcel build ./src/index.html --public-url ./src --out-dir build"
    },

    "devDependencies": {
        "babel-plugin-transform-runtime": "~6.23.0",
        "babel-polyfill": "~6.26.0",
        "babel-preset-env": "~1.6.1",
        "clang-format": "~1.2.2",
        "cross-env": "^5.1.6",
        "parcel-bundler": "~1.8.1"
    },
    "browserslist": [
    "> 1%",
        "ie >= 11",
        "ios >= 10",
        "android >= 6"
    ]
}
